# Project Rules & Guidelines for Aks Raag

## Versioning Rule
- Every time the Android app is compiled, built, or debugged, automatically increment the version string (`versionName`) by `0.0.1` (e.g. `3.0.0` -> `3.0.1`) and increment `versionCode` by `1` in `frontend/android/app/build.gradle`.
